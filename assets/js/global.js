$(document).ready(function() {
    // scrollbar
    $('.scrollbar-dynamic').scrollbar();
});

//run on every window resize
$(window).resize(function () {
    // scrollbar
    $('.scrollbar-dynamic').scrollbar();
});